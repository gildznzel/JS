var letter = document.getElementById("myInput").value;
var text;

// If the letter is "c"
if (letter >= "c") {
  text = "Spot on! Good job!";

// If the letter is anything else
} else {
  text = "Can do better.";
}
